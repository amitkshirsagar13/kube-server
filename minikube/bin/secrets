#!/bin/sh
if [ ! -z "$1" ]
then
        if [ ! -z "$2" ]
        then
                kubectl describe secrets --namespace $1 $2
        else
                kubectl get secrets -n $1
        fi
else
        kubectl get secrets --all-namespaces
fi
